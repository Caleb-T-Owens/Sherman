{
  "name": "pi-local-plugins",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && bun build ./src/*.ts --outdir ./dist --target node --format esm",
    "typecheck": "tsc --noEmit --project tsconfig.json"
  },
  "devDependencies": {
    "@mariozechner/pi-coding-agent": "*",
    "typescript": "^5.9.2"
  }
}
